# BUGS.md

# Найденные дефекты

| №  | Краткое описание                                      | Шаги воспроизведения                                                                 | Фактический результат                                                                                     | Ожидаемый результат                                                                                     | Серьёзность | Окружение               |
|----|------------------------------------------------------|-------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------|-------------------------------------------------------------------------------------------------------|-------------|------------------------|
| 1  | GET /item/{id} возвращает массив вместо объекта      | 1. Создать объявление через POST /item<br>2. Вызвать GET /item/{id} с этим id       | Возвращается массив с одним объектом вместо объекта: `[{"id": "...", "name": "..."}]`                   | Ожидается один объект с полями id, name, price, statistics                                           | Средняя     | QA API, Task2          |
| 2  | GET /statistic/{id} возвращает массив вместо объекта | 1. Создать объявление через POST /item<br>2. Вызвать GET /statistic/{id}           | Возвращается массив с объектом статистики вместо объекта: `[{"likes": 1, "viewCount": 1, "contacts":1}]` | Ожидается объект с полями likes, viewCount, contacts                                                 | Средняя     | QA API, Task2          |
| 3  | POST /item требует поле `statistics`                 | 1. Попытаться создать объявление без поля statistics                                 | Ошибка 400: `{"result":{"message":"поле likes обязательно"},"status":"400"}`                             | API должно корректно обработать отсутствие statistics или выдавать понятную валидацию             | Средняя     | QA API, Task2          |
